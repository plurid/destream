// #region imports
    // #region libraries
    import styled from 'styled-components';
    // #region libraries
// #region imports



// #region module
export const StyledEvents = styled.div`
    max-height: 250px;
    overflow: scroll;
    text-align: left;
`;

export const StyledEvent = styled.div`
    margin: 1rem 0;

    pre, code {
        font-size: 0.8rem;
        word-break: break-all;
        white-space: pre-wrap;
    }
`;

export const StyledEventHeader = styled.div`
    display: flex;
    gap: 0.5rem;
`;

export const StyledEventsText = styled.div`
    display: flex;
    gap: 0.5rem;
    align-items: center;
`;
// #region module
